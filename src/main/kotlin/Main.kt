package org.example

import java.io.RandomAccessFile

fun main() {
    val manager: EmployeeManager = EmployeeManager("test")

    TODO("CLI DONE IN A WHILE(TRUE) LOOP - WIP")
}

fun readValidUserInput(prompt: String, errorMsg: String): String{
    var result: String

    do{
        TODO("WIP")
    }while(result.isNullOrEmpty())

    return result
}
